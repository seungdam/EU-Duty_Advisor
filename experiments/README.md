# Experiments

This directory holds exploratory or retired checks that are intentionally not
collected by the CI pytest suite. `legacy/canonical_binding_canary.py` depends
on an experimental module that is not part of the runtime application.
